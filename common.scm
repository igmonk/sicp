;; Some common definitions used globally across all the chapters.

(define (square x)
  (* x x))

(define (cube x)
  (* x x x))

(define (inc n)
  (+ n 1))

(define (dec n)
  (- n 1))

(define (identity x) x)
